server:
  port: 8080

monitor:
  app-secret: abc12345

# 👇 InfluxDB 配置
influx:
  url: http://localhost:8086
  token: GCqeNNPRnEarrVZPlQVb116O43O_9if2q6FlIj-OR8SxLLkMmB3Pg2kCBtQZzeS3_2-EiWnKGbjt4LaQvIL7Cw== #InfluxDB Token
  org: my-org
  bucket: monitor_bucket

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 👇 改成你的数据库地址
    url: jdbc:mysql://localhost:3306/monitor_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 1234
  data:
    redis:
      # Redis在网络上默认使用明文传输,如果对数据敏感的,开发环境推荐使用将本地6379端口SSH隧道连接到远程6379端口,生成环境则推荐开启Redis TLS/SSL
      host: ${REDIS_HOST} #填写你的Redis Host(主机名)
      port: 6379
      password: ${REDIS_PASSWORD} #填写你的Redis Password(密码)
      database: 0
      timeout: 5000ms
      # 👇 这里是连接池配置 (Lettuce)
      lettuce:
        pool:
          # 最大连接数，默认8，生产环境建议调大，如 32 或 64
          max-active: 32
          # 最大空闲连接，保持一些连接随时可用
          max-idle: 8
          # 最小空闲连接
          min-idle: 0
          # 从池中获取连接的最大等待时间，配置为负值表示无限等待
          max-wait: 10000ms

# MyBatis-Plus 配置 (可选，用于打印 SQL 日志，方便调试)
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jwt:
  secret: MonitorSystemSecurityKeyMustBeVeryLongAndComplex2024!

aliyun:
  oss:
    endpoint: oss-cn-shenzhen.aliyuncs.com  # 你的 Bucket 所在地域节点
    access-key-id: ${ALIYUN_ACCESS_KEY_ID}         # RAM 用户的 Key
    access-key-secret: ${ALIYUN_ACCESS_KEY_SECRET} # RAM 用户的 Secret
    bucket-name: server-monitor-xu               # Bucket 名称
    # 访问域名前缀 (Bucket名.节点)
    url-prefix: https://server-monitor-xu.oss-cn-shenzhen.aliyuncs.com/